PREDICT_INTERVAL_MS = 1000   # интервал между предсказаниями в миллисекундах
STREAM_OUTPUT_CSV = './results_predict_dd/interval_predictions.csv'

MODEL_PATH = './saved_models/SVR_dataset1_DD.pkl'
DATA_PATH = './dataset.xlsx'
OUTPUT_CSV = './results_predict_dd/predictions_results.csv'

MODELS_DIR = './saved_models'

OUTPUT_PREDICTIONS_CSV = './results_predict_dd/models_predictions_comparison.csv'
OUTPUT_METRICS_CSV = './results_predict_dd/models_metrics_comparison.csv'
OUTPUT_TIME_PLOT = './results_predict_dd/models_time_comparison.png'

ROW_START = 1

# По какому таргету сравнивать модели: 'MFR' или 'DD'
TARGET = 'DD'

MFR_OBS_COL = 1
DD_COL = 2

FEATURES = [1, 2]

MFR_ERR_COL = 12
DD_ERR_COL = 13

HAS_TARGET = True   # True - есть реальные значения target, False - только предсказание без метрик
X_SCALER_PATH = './saved_scalers/X_scaler_ds1.pkl'
Y_SCALER_PATH = './saved_scalers/y_scaler_ds1_DD.pkl'

DRAW_PLOTS = True
VERBOSE = True
NUM_RUNS = 10

USE_NORMALIZATION = True


NORMALIZATION_CONFIG = {
    'SVR_dataset1_MFR': {
        'use_normalization': True,
        'x_scaler_path': './saved_scalers/X_scaler_ds1.pkl',
        'y_scaler_path': './saved_scalers/y_scaler_ds1_MFR.pkl'
    },

    'SVR_dataset1_DD': {
        'use_normalization': True,
        'x_scaler_path': './saved_scalers/X_scaler_ds1.pkl',
        'y_scaler_path': './saved_scalers/y_scaler_ds1_DD.pkl'
    },

    'SVR_dataset1_poly_MFR': {
        'use_normalization': True,
        'x_scaler_path': './saved_scalers/X_scaler_poly.pkl',
        'y_scaler_path': './saved_scalers/y_scaler_poly_MFR.pkl',
        'use_poly': True,
        'poly_degree': 5
    },

    'SVR_dataset1_poly_DD': {
        'use_normalization': True,
        'x_scaler_path': './saved_scalers/X_scaler_poly.pkl',
        'y_scaler_path': './saved_scalers/y_scaler_poly_DD.pkl',
        'use_poly': True,
        'poly_degree': 5
    },

    'GradientBoostingRegressor': {
        'use_normalization': False,
        'use_poly': False
    },

    'RandomForestRegressor': {
        'use_normalization': False,
        'use_poly': False
    },

    'ExtraTreesRegressor': {
        'use_normalization': False,
        'use_poly': False
    },

    'LinearTreeRegressor': {
        'use_normalization': False,
        'use_poly': False
    }
}

DEFAULT_USE_NORMALIZATION = False
DEFAULT_USE_POLY = False

VERBOSE = True
